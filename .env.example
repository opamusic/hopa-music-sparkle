# Copy to .env.local for local dev. Production reads this from a
# GitHub Actions variable (Repo settings → Secrets and variables → Actions → Variables).
VITE_API_BASE_URL=https://opa-airtable-proxy.<your-subdomain>.workers.dev
